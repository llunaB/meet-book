export default class User {
    constructor(nickname, email, password, passwordConfirm) {
        this.nickname = nickname
        this.email = email
        this.password = password
        this.passwordConfirm = passwordConfirm
    }
  }